# Signature image

Signature image data is environment-specific and is intentionally not
committed. Configure it through protected application data rather than adding
an actual signature image to source control.
